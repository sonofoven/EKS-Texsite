apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: nginx-texsite
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: nginx-texsite
  policy:
    alphabetical:
      order: asc

# Picks the latest image for automatic update
