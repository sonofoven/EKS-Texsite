apiVersion: v1
kind: Service
metadata:
  name: nginx-texsite
  namespace: default
spec:
  selector:
    app: nginx-texsite
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: metrics
      port: 8080
      targetPort: 8080
  type: ClusterIP
